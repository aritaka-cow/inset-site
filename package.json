{
  "name": "inset-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.14.0"
  },
  "scripts": {
    "dev": "ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "check": "ASTRO_TELEMETRY_DISABLED=1 astro check && node scripts/validate-content.mjs",
    "prepare:legacy-assets": "node scripts/prepare-legacy-assets.mjs",
    "build:site": "npm run prepare:legacy-assets && node scripts/validate-content.mjs && node scripts/validate-www-redirect.mjs && npm run validate:redirects && npm run test:redirects && ASTRO_TELEMETRY_DISABLED=1 astro check && ASTRO_TELEMETRY_DISABLED=1 astro build && node scripts/validate-build.mjs",
    "build": "PUBLIC_SITE_ENV=production npm run build:site",
    "build:preview": "PUBLIC_SITE_ENV=preview npm run build:site",
    "build:github": "PUBLIC_SITE_ENV=production SITE_BASE=/inset-site OUT_DIR=dist-github npm run build:site",
    "preview": "npm run build:preview && WRANGLER_SEND_METRICS=false wrangler dev",
    "guard:production-deploy": "node scripts/guard-production-deploy.mjs",
    "validate:redirects": "node scripts/validate-redirects.mjs public/_redirects",
    "test:redirects": "node --test scripts/redirect-contracts.test.mjs",
    "verify:live-redirects": "node scripts/verify-live-redirects.mjs",
    "deploy": "npm run guard:production-deploy && npm run build && npm run _deploy:site && npm run verify:live-redirects && npm run _deploy:www",
    "deploy:site": "npm run guard:production-deploy && npm run build && npm run _deploy:site && npm run verify:live-redirects",
    "deploy:www": "npm run guard:production-deploy && node scripts/validate-www-redirect.mjs && npm run _deploy:www",
    "deploy:ci": "node scripts/validate-redirects.mjs dist/_redirects && npm run _deploy:site && npm run verify:live-redirects",
    "_deploy:site": "WRANGLER_SEND_METRICS=false wrangler deploy --strict",
    "_deploy:www": "WRANGLER_SEND_METRICS=false wrangler deploy --config wrangler.www.jsonc --strict",
    "deploy:preview": "npm run build:preview && WRANGLER_SEND_METRICS=false wrangler versions upload",
    "deploy:dry-run": "npm run guard:production-deploy && npm run build && WRANGLER_SEND_METRICS=false wrangler deploy --dry-run --strict && WRANGLER_SEND_METRICS=false wrangler deploy --config wrangler.www.jsonc --dry-run --strict",
    "validate": "node scripts/validate-content.mjs && node scripts/validate-www-redirect.mjs && npm run validate:redirects && npm run test:redirects && node scripts/validate-build.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "astro": "7.0.9",
    "html-validate": "11.5.6",
    "typescript": "5.9.3",
    "wrangler": "4.111.0"
  }
}
